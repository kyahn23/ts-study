const person: {
  name: string;
  age: number;
  hobbies: string[];
  role: [number, string];
} = {
  name: "kyahn",
  age: 30,
  hobbies: ["Sports", "Game"],
  role: [2, "author"],
};

// person.role.push("Admin");
// person.role[1] = 10;

// person.role = [0, "admin", "user"];
console.log(person);
